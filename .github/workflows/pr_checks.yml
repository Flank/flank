name: "Check Pull Request"
on:
  pull_request:
    branches: test_pr_title
    types:
      - opened
      - edited
      - synchronize

jobs:
  main:
    runs-on: macos-latest
    steps:
      - uses: amannn/action-semantic-pull-request@v1.2.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
